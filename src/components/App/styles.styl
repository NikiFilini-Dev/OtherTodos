calendarWidth = 250px
sideBarWidth = 250px
mainWidth = calc(100% - 16px - 250px - 16px - 250px)

mainBlockRadius = 8.5px
mainBlockPadding = 36px 24px

brand = #2F45C5
accent = #FB2B76

@import "../../assets/fonts/Gilroy/stylesheet.css"

:root
  --brand #2F45C5
  --accent #FB2B76

*
  box-sizing border-box

body
  margin 0
  position absolute
  width 100vw
  height 100vh
  overflow hidden
  //background #9c9c9cd9

:global(#app)
  padding 24px
  padding-top 0
  font-family SF Pro Display, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif
  //background #F7F7F7
  height calc(100vh - 36px)
  //background #ffffff29
  position absolute
  left 0
  bottom 0
  width 100vw

.app
  display flex
  height: 100%

.sideBar
  background white
  padding: mainBlockPadding
  width 250px
  border-radius 8.5px
  overflow-y auto

.main
  padding 36px 16px
  background white
  border-radius 8.5px
  margin 0 16px
  width mainWidth

.timeline
  width 346px
  background white
  border-radius 8.5px
  padding mainBlockPadding

*::-webkit-scrollbar {
  width: 3px;
}

*::-webkit-scrollbar-track {
  //background white
  position relative
}

*::-webkit-scrollbar-thumb {
  width 3px
  border-radius 5px
  background #DCDFE5
}