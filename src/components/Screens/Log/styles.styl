.screen
  height: 100%
  max-height 100%
  display flex
  flex-direction column
  overflow-y auto

.head
  padding 21px 26px
  background white
  border-radius 24px
  // border 1px solid rgba(0,0,0,.1)
  margin-bottom 24px

.info
  margin-bottom 26px
  display flex
  align-items center
  justify-content space-between

.title
  font-weight 600
  font-size 24px
  margin-right 12px
  line-height 1em

.listOfLists
  display flex
  flex-direction column
  overflow-y auto
  height 100%