.screen
  display flex
  flex-direction column
  max-height 100%
  height 100%

.info
  margin-bottom 28px
  padding 0 20px
  display flex
  justify-content space-between

.title
  font-weight 600
  font-size 24px
  margin-right 12px

.list
  display flex
  flex-direction column
  height 100%
  overflow-y auto
  padding 0 20px

.project
  padding 6px
  border-bottom 1px solid rgba(0,0,0,0.04)